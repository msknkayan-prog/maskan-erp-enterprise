# مسكن الكيان ERP — إصدار الإنتاج v1.2

هذا الإصدار يدمج إصلاح علاقة المشاريع مع مدير المشروع، وإصلاح سجل الاعتمادات، ويضيف تشديد صلاحيات قاعدة البيانات للإنتاج.

## قبل الاعتماد الرسمي
1. شغّل `sql/06_production_security_hardening.sql` مرة واحدة من Supabase SQL Editor بدور postgres.
2. شغّل `sql/07_production_security_verify.sql`.
3. يجب أن تكون جميع أسطر RLS بحالة `PASS` وألا تظهر أي أسطر من نوع `anon_privilege`.
4. نفّذ اختبار دورة العمل: جهة → مشروع → عقد → طلب صرف → مستخلص → دفعة.

## ملاحظات أمان
- مفتاح Supabase Publishable Key الموجود في الواجهة مصمم للاستخدام العام؛ الحماية الحقيقية تعتمد على RLS والسياسات.
- لا تستخدم `service_role` داخل ملفات الواجهة أو متغيرات Vercel المتاحة للمتصفح.
- احتفظ بمشروع Supabase وVercel وGitHub مع تفعيل المصادقة الثنائية للحسابات الإدارية.
